tools:
  foobar:
    title: Foo Bar
    description: A dummy tool to exemplify the YAML file
    # removed this field - version: 0.1 # version should be provided by Git tags (e.g. v0.1.0)
    parameters:
      foo_int: 
        type: integer
      foo_float:
        type: float
      foo_string:
        type: string
      foo_enum:
        type: enum
        values:
          - foo
          - bar
          - baz
      foo_array:
        type: integer
        array: true
      foo_matrix:
        type: asset # changed from file
      foo_csv:
        type: asset # changed from file
